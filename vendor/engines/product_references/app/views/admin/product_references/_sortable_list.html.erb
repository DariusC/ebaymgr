<ul id='sortable_list'>
  <%= render :partial => 'product_reference', :collection => @product_references %>
</ul>
<%= render :partial => "/shared/admin/sortable_list",
            :locals => {
              :continue_reordering => (local_assigns.keys.include?(:continue_reordering)) ? continue_reordering : true
            } %>
